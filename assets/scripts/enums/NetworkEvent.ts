export enum NetworkEvent {
    CREATE_ROOM = 'create-room',
    JOIN_ROOM = 'join-room',
    LEAVE_ROOM = 'leave-room',
    BET = 'bet',
    GAME_ACTION_HIT = 'game-action-hit',
    GAME_ACTION_STAND = 'game-action-stand',
    GAME_ACTION_DOUBLE = 'game-action-double',
    GAME_ACTION_SPLIT = 'game-action-split',
    GAME_ACTION = 'game-action',
    CHIP_ENTRY_READY = 'chip-entry-ready',
    INSURANCE_RESPONSE = 'insurance-response',
    UPDATE_BALANCE = 'update-balance',
    LOGIN = 'login',
    LOGOUT = 'logout',
    GET_ROOM = "get-room",
    CREATE_ROOM_RESPONSE = 'create-room-response',
    JOIN_ROOM_RESPONSE = 'join-room-response',
    LEAVE_ROOM_RESPONSE = 'leave-room-response',
    PHASE_CHANGE = 'phase-change',
    TURN_CHANGE = 'turn-change',
    BET_REPONSE = 'dealer-turn-end',
    GAME_STARTED = 'bet-response',
    DEAL_CARD = 'deal-card',
    REVEAL_CARD = 'reveal-card',
    ACTION_RESPONSE = 'action-response',
    INSURANCE_OFFER = 'insurance-offer',
    INSURANCE_OFFER_RESPONSE = 'insurance-offer-response',
    GAME_RESULT = 'game-result',
    BALANCE_UPDATE = 'balance-update'
}